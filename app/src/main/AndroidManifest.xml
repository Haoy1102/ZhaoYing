<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhaoying_v13">

    <permission-group android:name="${applicationId}.andpermission" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZhaoYingv13">
        <activity
            android:name=".ui.home.cameraDec.CameraDecActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.CourseDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_course_detail"
            android:theme="@style/Theme.ZhaoYingv13.NoActionBar" />
        <activity
            android:name=".ui.home.detection.DetectionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.myInfo.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.cameraX.CameraXActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>